on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

name: rchk

jobs:
  rchk:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: r-lib/actions/run-rchk@master
      with:
        apt: libfftw3-dev libtiff-dev
